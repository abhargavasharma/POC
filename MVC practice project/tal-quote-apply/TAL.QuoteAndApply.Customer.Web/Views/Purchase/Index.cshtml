@using TAL.QuoteAndApply.Analytics.Models

@{
    ViewBag.Title = "Purchase Cover";
    ViewBag.AppName = "Purchase";
    ViewBag.StepIndex = 4;
    ViewBag.PageCssClass = "page-purchase";
}

@section Analytics
{
    @{
        Html.RenderAction("Header", "Analytics", new { model = new AnalyticsPageModel("PurchaseCoverPage", ViewBag.Title, "Quote", DateTime.Now, DateTime.Now) });
    }
}

<section class="tal-section-purchase">
    @{
        Html.RenderPartial("~/Views/Shared/_PageHeader.cshtml");
    }
    <div class="container">
        <main class="tal-layout-main row">
            <tal-purchase-form></tal-purchase-form>
        </main>
    </div>
</section>


